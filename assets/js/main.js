let person = {
    name: "mohammad alzibak",
    alter: "24",
    sagNameAlter: () => {
        console.log(`my name is ${person.name} my age is ${person.alter}`)
        alert(name, alter)
    }
}
person.sagNameAlter()